<p id="notice"><%= notice %></p>

<h1 class="site-heading-upper text-primary">Users</h1>

<div class="table-responsive-md">
<table class="table table-dark table-striped">
  <thead>
    <tr>
      <th scope="col">First Name</th>
      <th scope="col">Last Name</th>
      <th scope="col">email</th>
      <th scope="col">admin</th>
      <th scope="col">Competitions owned</th>
      <th scope="col">Competitions joined</th>
      <th scope="col" colspan="2"></th>
    </tr>
  </thead>

  <tbody>
    <% @users.each do |user| %>
      <tr scope="row">
        <td><%= user.f_name %></td>
        <td><%= user.l_name %></td>
        <td><%= user.email %></td>
        <td><%= user.admin ? "Yes" : "No" %></td>
        <td><%= user.comps.length %></td>
        <td><%= user.competitions.length %></td>
        <td><%= link_to 'Show', user %></td>
        <td><%= link_to 'Edit', edit_user_path(user) %></td>
      </tr>
    <% end %>
  </tbody>
</table>
</div>
<br>

<%= link_to 'New Competition', new_competition_path %>
